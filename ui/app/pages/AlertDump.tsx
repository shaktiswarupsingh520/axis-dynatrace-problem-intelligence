import React, { useState } from 'react';
import { useAppFunction } from '@dynatrace-sdk/react-hooks';
import { Flex } from '@dynatrace/strato-components/layouts';
import { Heading, Paragraph } from '@dynatrace/strato-components/typography';
import { useNavigate } from 'react-router-dom';

type Row = Record<string, unknown>;
interface Response { rows: Row[]; count: number; generatedAt: string; }
const v=(x:unknown)=>Array.isArray(x)?x.join('; '):x==null?'':String(x);
const fields=['display_id','event.name','event.status','event.severity','event.category','dt.davis.impact_level','event.start','event.end','affected_entity_names','root_cause_entity_id','event.description'];
const heads=['Problem ID','Title','Status','Severity','Category','Impact','Started','Ended','Affected Entities','Root Cause Entity','Description'];
const makeCsv=(rows:Row[])=>{const esc=(x:unknown)=>`"${v(x).replace(/"/g,'""')}"`;return `\uFEFF${[heads.map(esc).join(','),...rows.map(r=>fields.map(f=>esc(r[f])).join(','))].join('\r\n')}`;};
export const AlertDump=()=>{const nav=useNavigate();const[range,setRange]=useState('24h');const[status,setStatus]=useState('ALL');const[severity,setSeverity]=useState('ALL');const[zone,setZone]=useState('ALL');const req={from:`now-${range}`,to:'now',status,severity,managementZone:zone,limit:1000};const{data,isLoading,error,refetch}=useAppFunction<Response>({name:'getAlertDump',data:req});const rows=data?.rows??[];const download=()=>{const url=URL.createObjectURL(new Blob([makeCsv(rows)],{type:'text/csv;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=`dynatrace-problem-alert-dump-${new Date().toISOString().slice(0,10)}.csv`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);};return <Flex flexDirection="column" padding={24} gap={16}><style>{`.adbar{display:flex;gap:10px;align-items:end;flex-wrap:wrap;padding:14px;border:1px solid #dce3ea;border-radius:10px;background:#f7f9fb}.adbar label{display:flex;flex-direction:column;gap:5px;font-size:11px;font-weight:700}.adbar select,.adbar button{height:36px;border:1px solid #c8d2dd;border-radius:7px;padding:0 10px;background:#fff}.adbar button{font-weight:700;cursor:pointer}.adprimary{background:#174a7e!important;color:#fff;border-color:#174a7e!important}.adwrap{overflow:auto;border:1px solid #dce3ea;border-radius:10px}.adtable{width:100%;min-width:1500px;border-collapse:collapse;font-size:12px}.adtable th,.adtable td{padding:9px 11px;border-bottom:1px solid #e7ebef;text-align:left;vertical-align:top}.adtable th{position:sticky;top:0;background:#edf3f8;z-index:1;white-space:nowrap}.admeta{display:flex;justify-content:space-between;font-size:12px;color:#65758a}`}</style><Heading>Dynatrace Alert Dump</Heading><Paragraph>Live Davis problem export cloned from the Capacity Planner. The original Capacity Planner is untouched.</Paragraph><div className="adbar"><label>Time range<select value={range} onChange={e=>setRange(e.target.value)}><option value="1h">Last 1 hour</option><option value="6h">Last 6 hours</option><option value="24h">Last 24 hours</option><option value="7d">Last 7 days</option><option value="30d">Last 30 days</option></select></label><label>Status<select value={status} onChange={e=>setStatus(e.target.value)}><option value="ALL">All</option><option value="ACTIVE">Active</option><option value="CLOSED">Closed</option></select></label><label>Severity<select value={severity} onChange={e=>setSeverity(e.target.value)}><option value="ALL">All severities</option><option value="1">Level 1</option><option value="2">Level 2</option><option value="3">Level 3</option><option value="4">Level 4</option><option value="5">Level 5</option></select></label><label>Management Zone<select value={zone} onChange={e=>setZone(e.target.value)}><option value="ALL">All Management Zones</option>{zone!=='ALL'&&<option value={zone}>{zone}</option>}</select></label><button className="adprimary" type="button" onClick={()=>void refetch()} disabled={isLoading}>{isLoading?'Loading…':'Load problems'}</button><button type="button" onClick={download} disabled={!rows.length}>Download CSV</button><button type="button" onClick={()=>nav('/')}>Back to Problems</button></div>{error&&<Paragraph>Unable to load alert dump: {error.message}</Paragraph>}<div className="admeta"><span>{rows.length} loaded · maximum 1,000 · duplicates excluded</span><span>{data?.generatedAt?new Date(data.generatedAt).toLocaleString():''}</span></div><div className="adwrap"><table className="adtable"><thead><tr>{heads.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.length?rows.map((r,i)=>{const s=new Date(v(r['event.start']));return <tr key={`${v(r.display_id)}-${i}`}><td><strong>{v(r.display_id)}</strong></td><td>{v(r['event.name'])}</td><td>{v(r['event.status'])}</td><td>{v(r['event.severity'])}</td><td>{v(r['event.category'])}</td><td>{v(r['dt.davis.impact_level'])}</td><td>{Number.isNaN(s.getTime())?v(r['event.start']):s.toLocaleString()}</td><td>{v(r['event.end'])}</td><td>{v(r.affected_entity_names||r.affected_entity_ids)}</td><td>{v(r.root_cause_entity_id)||'Not returned'}</td><td>{v(r['event.description'])}</td></tr>}) : <tr><td colSpan={heads.length}>{isLoading?'Reading Davis problems…':'No problems matched the selected filters.'}</td></tr>}</tbody></table></div></Flex>;};
